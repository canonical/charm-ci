[tox]
skipsdist = True

[testenv:integration]
deps =
    pytest
    jubilant
    {env:OPCLI_PACKAGE:opcli}
commands =
    pytest {posargs:tests/integration/} -v
passenv =
    HOME
    JUJU_*
    KUBECONFIG
    OPCLI_ARTIFACTS_BUILD_YAML
