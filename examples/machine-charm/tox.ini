[tox]
skipsdist = True

[testenv:integration]
deps =
    pytest
    jubilant
    opcli @ file://{toxinidir}/../../
commands =
    pytest {posargs:tests/integration/} -v
passenv =
    HOME
    JUJU_*
    KUBECONFIG
